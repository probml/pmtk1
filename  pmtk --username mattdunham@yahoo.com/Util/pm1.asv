function y = pm1(y)
% Ensure y(i) in {-1,+1}
% Input can be in {0,1} or {1,2}

if length(find(y==0)>0)
  
\   Y(Y==2) = 1;   % turn into a binary classification problem by combining classes 1 and 2
      Y(Y==3) = 2;
      %[nexamples,nfeatures] = size(X);
      %ntrain = floor(0.8*nexamples);
      %ntest = nexamples - ntrain;
      ypm = Y;
      ypm(Y == 2) = -1;    
      